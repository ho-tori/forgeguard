# 规格过程记录

## 过程说明

2026-08-12，项目所有者明确要求“阅读文档，直接做，不需要头脑风暴”。因此本项目没有伪造 Superpowers brainstorming 的问答或签字过程；这是对课程通用要求 §4.1 的显式偏离。AI 助手先阅读两份课程要求，再依据可测试性判据生成 SPEC/PLAN。所有仍需项目所有者亲自完成的部分（身份、远端仓库、真实 CI、个人反思）保留为明确 TODO。

## 三轮关键规格迭代（基于文档约束审查，不冒充用户对话）

### 迭代 1：从“能写代码”收敛到“治理可验证”

- 初始解释：实现读写和运行测试的通用 coding agent。
- 文档暴露的问题：六维度都浅做会成为简单 API 封装；危险动作不能只写在 prompt。
- 决策：选择治理作为重点，增加路径解析围栏、命令词法分析、风险分类、一次性审批票据、哈希链审计。
- 关键变化：验收标准从“模型不执行危险命令”改为“直接构造 Action，在无 LLM 下确定性验证未触达工具”。

### 迭代 2：从“环境变量 key”改为 OS/secret 双路径

- 初始解释：从 `OPENAI_API_KEY` 读取即可。
- 文档暴露的问题：环境变量只是来源且进程可见，首次运行还需录入/更新/清除。
- 决策：桌面 Windows 使用 Credential Manager，容器使用只读 secret file；环境变量仅作明确标险的兼容选项。CLI 和 Web 状态不得回显。
- 关键变化：增加凭据威胁模型、容器挂载流程和秘密脱敏测试。

### 迭代 3：从“测试工具”改为闭环完成门禁

- 初始解释：agent 可以选择运行测试，也可以直接 finish。
- 文档暴露的问题：反馈若不能影响停机，只是工具列表，不是闭环。
- 决策：配置要求验证时，`finish` 必须以当前变更之后的成功反馈为前置条件；失败观察结构化回灌下一轮。Mock 脚本固定复现失败→修正→成功。
- 关键变化：新增反馈新鲜度和完成门禁验收标准。

## AI 建议与人工决策状态

- AI 建议并采用到草案：标准库优先、治理为主贡献、SQLite 有界记忆、哈希链审计、单进程 Web 控制面。
- 用户明确推翻：brainstorming 前置流程；按指示直接执行并如实记偏离。
- 尚待项目所有者评审：产品名称、真实供应商 endpoint、公开部署方式、是否接受 WebUI 只做单用户控制面。

## 冷启动验证

课程要求使用“不同类型、全新 session”的第二智能体，只提供 SPEC+PLAN。当前会话既未获用户授权创建 subagent，又被明确要求直接做；因此没有伪造冷启动结果。项目所有者应在提交前执行：用另一种 agent 打开全新会话，仅提供 `SPEC.md` 和 `PLAN.md`，让其完成 Task 2 中的审批子任务，遇到不确定即暂停。将真实问题、错误解读和修订 diff 追加于此。

建议观察点：

1. “危险命令”分类是否足以让陌生实现者无需猜测。
2. 审批摘要是否包含所有会改变语义的字段。
3. feedback 成功在文件修改后是否仍被错误复用。

## 对方法的初步评价

规格先行迫使安全边界从形容词变成输入、状态和断言，这是有效部分。固定要求“三轮对话”在用户已经做出明确范围决策时可能形式化；可靠证据应是陌生实现者暴露的歧义和相应 diff，而不是补写一段貌似完整的聊天记录。本文件只记录实际发生的过程。

## policy-check 增量功能的真实 brainstorming（2026-08-12）

这是对 policy-check 增量功能的真实 brainstorming，不是对原始项目过程的追溯补写。

本轮在检查现有仓库、`SPEC.md`、CLI、严格 Action parser、`PolicyEngine`、秘密脱敏实现及相关测试后，进行了以下真实问答和人工决策：

1. **Action JSON 的输入方式是什么？**
   - 决定：同时支持 `--action-json` 和 stdin；两者同时提供时作为输入错误。
2. **治理结论如何映射 CLI 退出码？**
   - 决定：`0=allow`、`2=require_approval`、`3=deny`、`4=输入或解析错误`。
3. **输入含疑似 API key、token 或密码时如何输出？**
   - 决定：仍完成解析与策略判断，但所有输出统一脱敏，且绝不回显原始 Action。
4. **允许检查哪些 Action？**
   - 决定：与现有严格解析器保持一致，支持 `read_file`、`write_file`、`run_command`、`run_feedback`、`remember`、`finish`。
5. **policy-check 是否读取现有 workspace 和 config？**
   - 决定：读取 `--workspace` 与 `--config`，仅据此构造 `PolicyEngine`；不初始化 service、审批库、审计、记忆、凭据或工具。
6. **输入、解析或配置错误如何输出？**
   - 决定：stdout 输出稳定 JSON，stderr 为空，退出码为 `4`。
7. **合法 Action 的响应字段是什么？**
   - 决定：固定为 `verdict`、`reason`、`risk`；没有风险分类时 `risk` 为 `null`，不返回 Action 内容。

在方案比较中，人工批准采用“纯服务函数 + 薄 CLI 适配层”：服务函数只调用 `parse_action()` 和 `PolicyEngine.evaluate()`；CLI 只负责输入、配置、JSON 序列化和退出码。该设计边界明确保证 policy-check 不创建或消费 approval，不调用 `ToolRegistry`，也不执行文件、命令、LLM 或网络动作。

2026-08-12，项目所有者在完成上述真实问答并选择方案 A 后明确批准设计，要求保存设计文档但暂时不要实现。批准后的设计保存在 `docs/superpowers/2026-08-12-policy-check-design.md`；截至本记录更新时尚未开始实现。
